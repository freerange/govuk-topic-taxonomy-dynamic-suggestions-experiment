Display the full taxon tree for each suggestion
  Use "Breadcrumbs" component from Design System?

Include similar documents by default?

Separate link to document on GOV.UK from heading?

Consistent use of "taxon" or "tag" in UI

Update README to explain what it does/how to get it running

Add README for https://github.com/freerange/gds-topic-suggestion

Add "Back links" using Design System?

Add a "Phase banner" to indicate this is in "alpha"?

Tidy up GOV.UK footer

---

Ensure we always recommend n? taxons (e.g. by not necessarily taking just the first 5 similar documents)

Use some distance threshold so that we don't include taxons from "similar" documents that aren't actually very similar

Add bookmarklet for Whitehall publisher so that publisher can request suggested taxons for a new document
