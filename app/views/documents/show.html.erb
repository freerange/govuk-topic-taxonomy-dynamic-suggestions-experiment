<% content_for(:title) do %>
  <%= @document.title %>
<% end %>

<% content_for(:breadcrumb) do %>
  <nav class="govuk-breadcrumbs" aria-label="Breadcrumb">
    <ol class="govuk-breadcrumbs__list">
      <li class="govuk-breadcrumbs__list-item">
        <%= link_to "Home", documents_path, class: "govuk-breadcrumbs__link" %>
      </li>
      <li class="govuk-breadcrumbs__list-item">
        <%= @document.title %>
      </li>
    </ol>
  </nav>
<% end %>

<h2 class="govuk-heading-m">
  <%= @document.title %>
</h2>

<% if @document.govuk_url.present? %>
  <p class="govuk-body">
    <%= link_to "View document on GOV.UK (opens in new tab)", @document.govuk_url, class: "govuk-link", rel: "noreferrer noopener", target: "_blank" %>
  </p>
<% end %>

<h3 class="govuk-heading-s">Existing topic taxonomy tags</h3>

<% if @document.taxons.any? %>
  <ol class="govuk-list govuk-list--number">
    <% @document.taxons.each do |taxon| %>
      <% taxon = Taxon.find_by(base_path: taxon['base_path']) %>
      <li><%= render partial: 'taxons', locals: { taxon: } %></li>
    <% end %>
  </ol>
<% else %>
  <p class="govuk-body">The document has no topic taxonomy tags.</p>
<% end %>

<h3 class="govuk-heading-s">Suggested topic taxonomy tags</h3>

<% if @suggested_taxons.any? %>
  <ol class="govuk-list govuk-list--number">
    <% @suggested_taxons.each do |title, taxons| %>
      <% taxon = Taxon.find_by(base_path: taxons.first['base_path']) %>
      <li><%= render partial: 'taxons', locals: { taxon: } %></li>
    <% end %>
  </ol>
<% else %>
  <p class="govuk-body">There are no other suitable topic taxonomy tags.</p>
<% end %>

<h3 class="govuk-heading-s">Similar documents</h3>
<p class="govuk-body">The documents from which the suggested topic taxonomy tags are derived.</p>

<ol class="govuk-list govuk-list--number">
  <% @similar_documents.each do |document| %>
    <li><%= link_to document.title, document_path(document), class: "govuk-link" %></li>
  <% end %>
</ol>
